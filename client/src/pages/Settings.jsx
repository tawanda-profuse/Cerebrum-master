import React from 'react';
import Navigation from '../components/Navigation';

const Settings = () => {
    return (
        <section>
            <Navigation />
            <h1>Settings</h1>
        </section>
    );
};

export default Settings;
